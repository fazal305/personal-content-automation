{
  "name": "personal-content-automation",
  "private": true,
  "version": "0.1.0",
  "description": "A personal content operating system: capture ideas, draft, review, schedule, and publish across platforms with real automation where free APIs allow it.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "db:migrate": "npm run db:migrate --workspace server"
  },
  "license": "MIT"
}
